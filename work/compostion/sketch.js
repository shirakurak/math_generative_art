
float angle;

function setup() {
  createCanvas(480, 340);
  frameRate(1);
  background(230, 215, 190, 70);
}

function draw() {

  // 原点をウィンドウの中心に
  translate(width/2, height/2);

  // angle = random(0, 360);
  print(rad(60));

  // x = random(width);
  // y = random(height);
  // w = random(0, 20);
  //
  // line(x, y, x+2*w, y);
  // line(x+w, y-w, x+w, y+w);

}
